/* AthletsManual Class (actual athlet data)
first_name first name of athlet
sur_name family name of atlet
corridor_done if he finished the corridor or not
line_done if he finished the finish line or not
*/

export class AthletsManual {
  first_name: string;
  sur_name: string;
  code_identifier: string;
  corridor_done: boolean;
  line_done: boolean;
}
